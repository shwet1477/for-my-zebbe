import { useState } from "react";
import { Button } from "@/components/ui/button";

export default function LoveGame() {
  const [started, setStarted] = useState(false);
  const [lovePoints, setLovePoints] = useState(0);
  const [finished, setFinished] = useState(false);

  const handleClick = () => {
    if (lovePoints < 10) {
      setLovePoints(lovePoints + 1);
    } else {
      setFinished(true);
    }
  };

  return (
    <div className="min-h-screen bg-pink-100 flex flex-col items-center justify-center p-6 text-center">
      <h1 className="text-4xl font-bold text-pink-600 mb-4">
        {finished ? "Surprise! 💖" : "Hi Bebe! 💕"}
      </h1>

      {!started && !finished && (
        <>
          <p className="text-lg text-pink-700 mb-4">
            I made something special for you... wanna see how much I love you? 💌
          </p>
          <Button onClick={() => setStarted(true)} className="bg-pink-500 hover:bg-pink-600 text-white text-lg">
            Start the Game 💘
          </Button>
        </>
      )}

      {started && !finished && (
        <>
          <p className="text-pink-700 text-xl mb-4">
            Click to find out how much I love you! 😘
          </p>
          <Button onClick={handleClick} className="bg-red-400 hover:bg-red-500 text-white text-lg">
            Click me! ❤️ ({lovePoints}/10)
          </Button>
        </>
      )}

      {finished && (
        <>
          <p className="text-pink-700 text-xl mb-4">
            I love you more than words can ever say. Forever and ever 💍💗
          </p>
          <img
            src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExcmZrcXdhZnI5NjczdThkdWh0OXE5dTljb3FiMjBvd3o1dThsbHM1dSZlcD12MV9naWZzX3NlYXJjaCZjdD1n/iKef9d47gZV68/giphy.gif"
            alt="Love Gif"
            className="rounded-xl shadow-xl w-60"
          />
        </>
      )}
    </div>
  );
}
